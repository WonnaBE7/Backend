<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wonnabe.auth.mapper.AuthMapper">

    <!-- 이메일 중복 여부 확인(회원가입 시) -->
    <select id="existsByEmail" resultType="int">
        SELECT COUNT(*)
        FROM User_Profile
        WHERE email = #{email}
          AND deleted_at IS NULL
    </select>

    <!-- 사용자 회원정보 추가(회원가입) -->
    <insert id="insertUserProfile">
        INSERT INTO User_Profile (user_id, name, email, password_hash, signup_type, created_at)
        VALUES (#{userId}, #{name}, #{email}, #{password}, #{signupType}, NOW())
    </insert>

</mapper>
