<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wonnabe.community.mapper.CommentMapper">

    <!-- 댓글 조회 -->
    <select id="selectCommentsByBoardId" resultType="CommentDTO">
        SELECT
            c.id AS commentId,
            u.name AS userName,
            f.name AS nowme,
            c.content,
            (SELECT COUNT(*) FROM comment_like cl WHERE cl.comment_id = c.id) AS likeCount
        FROM comment c
                 JOIN user_profile u ON c.user_id = u.user_id
                 JOIN user_info i ON u.user_id = i.user_id
                 JOIN financial_tendency_type f ON i.nowme_id = f.id
        WHERE c.parent_comment_id = #{boardId} AND c.post_id = #{communityId} AND c.is_deleted = 0
    </select>

    <!-- 댓글 생성 -->
    <insert id="insertComment">
        INSERT INTO comment (post_id, user_id, parent_comment_id, content, created_at)
        VALUES (#{postId}, #{userId}, #{parentCommentId}, #{content}, NOW())
    </insert>

    <!-- 댓글 삭제 -->
    <update id="markCommentAsDeleted">
        UPDATE comment
        SET is_deleted = 1
        WHERE id = #{commentId}
          AND parent_comment_id = #{boardId}
          AND user_id = #{userId}
    </update>


</mapper>
